/* Defining a Data Transfer Object (DTO) class for creating a new user */
export class CreateUserDTO {
    first_name: string;
    last_name: string;
    email_address: string;
    img_url: string;
}